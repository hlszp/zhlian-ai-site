# Backend configuration (copy to .env and adjust for local development)
ENVIRONMENT=development
DEBUG=false
DATABASE_URL=postgresql://zlai:zlai@localhost:5432/zlai

# Admin credentials for development /staging Basic Auth (production uses Nginx)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# CORS origins for local frontend dev server
CORS_ORIGINS=http://localhost:5173
